import React, {Component} from 'react';

export default class RouterPage extends Component {

  render() {
    return (
      <div>
        <span>React Router 是完整的 React 路由解决方案</span>
        <div>React Router 保持 UI 与 URL 同步。它拥有简单的 API 与强大的功能例如代码缓冲加载、动态路由匹配、以及建立正确的位置过渡处理。你第一个念头想到的应该是 URL，而不是事后再想起。</div>
      </div>
    )
  }
}
